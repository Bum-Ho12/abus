name: abus
description: >
  A-synchronous Business Unified System for Flutter state management.
  Supports optimistic updates with rollback and integrates with BLoC and Provider.
version: 0.0.2
homepage: https://github.com/Bum-Ho12/abus
repository: https://github.com/Bum-Ho12/abus
issue_tracker: https://github.com/Bum-Ho12/abus/issues

environment:
  sdk: ^3.6.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
